{
    "daily_limit": {
        "description": "每日抽取上限",
        "hint": "每位用户每天可以抽取老婆的次数上限",
        "type": "int",
        "slider": {
            "min": 1,
            "max": 10,
            "step": 1
        },
        "default": 3
    },
    "excluded_users": {
        "description": "用户排除列表",
        "hint": "抽老婆时, 在此列表中的QQ号将不会被抽中",
        "type": "list",
        "default": []
    }
}